using System;
using System.Collections.Generic;
using System.Text;
using System.IO;
using System.Linq;
using System.Web;
//using Utilidades.Cliente;
//using Utilidades.Localizacion;
/// Clase que se conecta con la base de dtos para realizar las transacciones respectivas
namespace Datos
{
    class Registro
    {
        private Mapeo db = new Mapeo();

        //public List<string> GetDepartamento()
        //{
           // List<Localizacion> localizacion = db.localizacion.ToList();

          //  foreach (string departamento in localizacion)
           // {
            //    Eventos evento = db.eventos.Where(x => x.Zona_id == id).FirstOrDefault();
///eventos.Add(evento);
///}
            //return;
       // }

    }
}
